import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<React.StrictMode><App /></React.StrictMode>);
```

8. Click **"Commit changes"**

9. One more — click **"Add file"** → **"Create new file"**
10. Type filename: **`src/App.js`**
11. Now this file is long — open the **`CyberDost_SourceCode.zip`** I gave you, go inside, open `src/App.js` with any text editor (Notepad, VS Code, etc.), copy ALL the content, and paste it here
12. Click **"Commit changes"**

After all 3 files are created, your repo should show:
```
├── package.json
├── public/
│   └── index.html
└── src/
    ├── App.js
    └── index.js
